% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/change_tag.R
\name{change_tag_merge}
\alias{change_tag_merge}
\title{Merge a from-tag into an already-existing to-tag.}
\usage{
change_tag_merge(
  str_from,
  str_to,
  df.tag_table,
  df.highlights,
  colname.tag_path = list(id = "id", path = "path"),
  colname.tag_id = "tag_id"
)
}
\arguments{
\item{str_from}{Length-1 character. The tag path to rename or merge away.}

\item{str_to}{Length-1 character. The target tag path.}

\item{df.tag_table}{Data frame of the \code{tag_table} table
(columns include \code{id} and \code{path}).}

\item{df.highlights}{Data frame of the \code{highlights} table
(columns include \code{tag_id}).}

\item{colname.tag_path}{Named list with \code{id} and \code{path} column names.}

\item{colname.tag_id}{Character. Column name of the tag id in \code{df.highlights}.}
}
\value{
List of length 2: updated \code{tag_table}, updated
  \code{highlights}.
}
\description{
Re-points every highlight with \code{tag_id == id_from} to
\code{tag_id == id_to}, and marks the from-tag's row as
\code{DEMOTED_<str_from>} so the tag drops out of subsequent review
passes but remains discoverable in the raw SQLite.
}
\details{
Uses exact string equality on \code{path} (not \code{gsub}) and integer
equality on \code{tag_id}, so paths containing regex metacharacters
(\code{|}, \code{.}, \code{*}, \code{[}, etc.) are safe.

Requires both \code{str_from} and \code{str_to} to appear as exactly one
row each in \code{df.tag_table$path}; stops with a clear error message
otherwise.
}
